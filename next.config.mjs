/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "akxcaxlcbnployplysxc.supabase.co",
        port: "",
      },
    ],
  },
};

export default nextConfig;
